idade = 18
possui_cracha = True
esta_bloqueado = False

print(f"idade >= 18 and possui_cracha: {idade >= 18 and possui_cracha}")
print(f"idade < 18 or possui_cracha: {idade < 18 or possui_cracha}")
print(f"not esta_bloqueado: {not esta_bloqueado}")
print(f"idade >= 18 and not esta_bloqueado: {idade >= 18 and not esta_bloqueado}")
print(f"possui_cracha and esta_bloqueado: {possui_cracha and esta_bloqueado}")